export interface LoginData {
    user: string;
    password: string;
}

export interface RegisterData {
    firstName: string;
    lastName: string;
    nickname: string;
    email: string;
    username: string;
    password: string;
    confirmPassword: string;
}